{
  "name": "sudoku-v",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "turbo run dev --parallel --filter=@sudoku-v/web --filter=@sudoku-v/mobile",
    "dev:web": "pnpm --filter @sudoku-v/web dev",
    "dev:mobile": "pnpm --filter @sudoku-v/mobile dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.6.0"
  }
}

